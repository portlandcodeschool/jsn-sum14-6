function fs.readFile(file, callbackFn){
	for (var i=0, i<file.Bytes, i++)
		while (!myturn()) {dawdle;}
		if (file.Bytes == bad){
			callbackFn(new Error('I found an error and stopped'));
		}
		var o += file[i]}
		callbackFn(null, o);
}