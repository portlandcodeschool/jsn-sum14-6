
function fs.readFile(file, callbackFn){
	for (var i=0, i<file.Bytes, i++)
		if (file.Bytes == bad){
			callbackfn("I found an error and stopped");
		}
		var o =+ file[i]}
		callbackFn(undefined, o);
}

var len = fs.readFile(process.argv[2], function (err, data){
	if (err) throw err;
	console.log(data.toString().split('\n').length-1);
});